<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:local="using:PropertyTools">

    <LinearGradientBrush x:Key="SelectionBrush" StartPoint="0,0" EndPoint="0,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#FFD45A" />
            <GradientStop Offset="1" Color="#FFEF71" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="SelectionBrush2" StartPoint="0,0" EndPoint="1,0">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#FFD45A" />
            <GradientStop Offset="1" Color="#FFEF71" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#dfe3e8" />

    <x:Double x:Key="DataGridColumnHeaderHeight">20</x:Double>
    <x:Double x:Key="DataGridRowHeaderWidth">40</x:Double>

    <Style TargetType="local:DataGrid">
        <Setter Property="RowsContextMenu">
            <Setter.Value>
                <MenuFlyout>
                    <!--<MenuFlyoutItem Text="Insert" Command="local:DataGridCommands.InsertRows"/>
                    <MenuFlyoutItem Text="Delete" Command="local:DataGridCommands.DeleteRows"/>-->
                </MenuFlyout>
            </Setter.Value>
        </Setter>
        <Setter Property="ColumnsContextMenu">
            <Setter.Value>
                <MenuFlyout>
                    <!--<MenuFlyoutItem Text="Insert" Command="local:DataGridCommands.InsertColumns"/>
                    <MenuFlyoutItem Text="Delete" Command="local:DataGridCommands.DeleteColumns"/>-->
                    <!--<MenuFlyoutSeparator/>-->
                    <!--<MenuFlyoutItem Text="Sort smallest to largest" Command="local:DataGridCommands.SortAscending"/>
                    <MenuFlyoutItem Text="Sort largest to smallest" Command="local:DataGridCommands.SortDescending"/>
                    <MenuFlyoutItem Text="Do not sort" Command="local:DataGridCommands.ClearSort"/>-->
                </MenuFlyout>
            </Setter.Value>
        </Setter>
        <Setter Property="SheetContextMenu">
            <Setter.Value>
                <MenuFlyout>
                    <!--<MenuFlyoutItem Command="ApplicationCommands.Cut"/>
                    <MenuFlyoutItem Command="ApplicationCommands.Copy"/>
                    <MenuFlyoutItem Command="ApplicationCommands.Paste"/>
                    <MenuFlyoutItem Command="ApplicationCommands.Delete"/>-->
                </MenuFlyout>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DataGrid">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid x:Name="PART_Grid">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            
                            <!-- The grid cell area (sheet) -->
                            <ScrollViewer x:Name="PART_SheetScrollViewer" IsTabStop="False" Grid.Row="1" Grid.Column="1"
                                          HorizontalScrollBarVisibility="Auto" 
                                          VerticalScrollBarVisibility="Auto">
                                <Grid x:Name="PART_SheetGrid" Background="Transparent">
                                    <Border x:Name="PART_SelectionBackground" Background="#b7dbff" />
                                    <Border x:Name="PART_CurrentBackground" Background="White" />
                                    <Border x:Name="PART_Selection" BorderBrush="Black" BorderThickness="3" Margin="-1,-1,-2,-2" />
                                    <Border x:Name="PART_AutoFillSelection" BorderBrush="Gray" BorderThickness="3" Margin="-1,-1,-2,-2" Visibility="Collapsed" />
                                    <Border x:Name="PART_AutoFillBox" Width="7" Height="7"
                                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                            Background="Black" BorderBrush="White" BorderThickness="1" Margin="0, 0, -4, -4" />
                                </Grid>
                            </ScrollViewer>
                            
                            <!-- Column headers -->
                            <ScrollViewer x:Name="PART_ColumnScrollViewer" Grid.Row="0" Grid.Column="1" 
                                          VerticalScrollBarVisibility="Hidden"
                                          HorizontalScrollBarVisibility="Hidden">
                                <Grid x:Name="PART_ColumnGrid" Height="{TemplateBinding ColumnHeaderHeight}"
                                      Background="{StaticResource HeaderBackgroundBrush}">
                                    <Border x:Name="PART_ColumnSelectionBackground" Background="{StaticResource SelectionBrush}" />
                                </Grid>
                            </ScrollViewer>
                            
                            <!-- Row headers -->
                            <ScrollViewer x:Name="PART_RowScrollViewer" Grid.Row="1" Grid.Column="0" 
                                          VerticalScrollBarVisibility="Hidden"
                                          HorizontalScrollBarVisibility="Hidden">
                                <Grid x:Name="PART_RowGrid" Width="{TemplateBinding RowHeaderWidth}"
                                      Background="{StaticResource HeaderBackgroundBrush}">
                                    <Border x:Name="PART_RowSelectionBackground" Background="{StaticResource SelectionBrush2}" />
                                </Grid>
                            </ScrollViewer>
                            
                            <!-- The top-left cell -->
                            <Grid Grid.Row="0" Grid.Column="0">
                                <Border x:Name="PART_TopLeft"
                                        Background="{StaticResource HeaderBackgroundBrush}"
                                        BorderBrush="#b1b5ba" BorderThickness="1,1,1,1" Margin="0,0,0,0">
                                    <Path Data="F1 M0,11 L11,11 L11,0 z" Fill="#30000000"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Bottom" Margin="3" />
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>